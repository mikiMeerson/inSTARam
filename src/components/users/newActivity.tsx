import { Typography } from '@mui/material';

const NewActivity = () => (
  <div className="newActivityWindow">
    <Typography variant="h6">
      פעילות חדשה באתר
    </Typography>
  </div>
);

export default NewActivity;
