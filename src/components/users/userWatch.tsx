import { Typography } from '@mui/material';

const UserWatch = () => (
  <div className="watchWindow">
    <Typography variant="h6">
      פעילות ברשימת הצפייה שלך
    </Typography>
  </div>
);

export default UserWatch;
